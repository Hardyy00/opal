type Props = {
  activeWorkspaceId: string;
};

const Sidebar: React.FC<Props> = ({ activeWorkspaceId }) => {
  return <div className="bg-[#111111] flex-none">Sidebar</div>;
};

export default Sidebar;
